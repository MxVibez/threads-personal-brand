FROM node:24-alpine AS build
WORKDIR /app
COPY miniapp/package.json miniapp/package-lock.json ./
RUN npm ci
COPY miniapp ./
RUN npm run build

FROM caddy:2-alpine
COPY infra/miniapp/Caddyfile /etc/caddy/Caddyfile
COPY --from=build /app/dist /srv
EXPOSE 8080
